// logic.js — funções puras de negócio
// Compatível com Node.js (Jest) e browser

// ---- funções serão adicionadas nas tasks seguintes ----

if (typeof module !== 'undefined' && module.exports) {
  module.exports = {};
}
